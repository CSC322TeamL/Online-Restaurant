<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manager">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Manager"
        android:usesCleartextTraffic="true">
        <activity android:name=".createDeliveryAccount"></activity>
        <activity android:name=".createChefAccount" />
        <activity android:name=".handle_disputecomplaint" />
        <activity android:name=".de_register" />
        <activity android:name=".staff_performance" />
        <activity android:name=".taboo_page" />
        <activity android:name=".create_account" />
        <activity android:name=".test" />
        <activity android:name=".compliment_detail" />
        <activity android:name=".complaint_detail" />
        <activity android:name=".complaints_and_compliments" />
        <activity android:name=".application" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>